# contracts/commands/

> **Submodule owner stub (M0.1).** Scaffold only — no logic yet.

- **Parent:** `contracts/`
- **Purpose:** Command contracts (intended transitions).
- **Accepted dependencies:** inherits `contracts/` rules (see parent README).
