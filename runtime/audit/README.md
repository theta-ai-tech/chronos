# runtime/audit/

> **Submodule owner stub (M0.1).** Scaffold only — no logic yet.

- **Parent:** `runtime/`
- **Purpose:** Causal-graph retention and actor/configuration attribution.
- **Accepted dependencies:** inherits `runtime/` rules (see parent README).
