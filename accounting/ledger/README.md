# accounting/ledger/

> **Submodule owner stub (M0.1).** Scaffold only — no logic yet.

- **Parent:** `accounting/`
- **Purpose:** Immutable balanced, idempotent postings.
- **Accepted dependencies:** inherits `accounting/` rules (see parent README).
