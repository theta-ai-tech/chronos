# adapters/external/

> **Submodule owner stub (M0.1).** Scaffold only — no logic yet.

- **Parent:** `adapters/`
- **Purpose:** Deferred external-observation adapters (reserved).
- **Accepted dependencies:** inherits `adapters/` rules (see parent README).
