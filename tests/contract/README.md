# tests/contract/

> **Submodule owner stub (M0.1).** Scaffold only — no logic yet.

- **Parent:** `tests/`
- **Purpose:** Producer/consumer conformance tests.
- **Accepted dependencies:** inherits `tests/` rules (see parent README).
