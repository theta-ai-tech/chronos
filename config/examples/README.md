# config/examples/

> **Submodule owner stub (M0.1).** Scaffold only — no logic yet.

- **Parent:** `config/`
- **Purpose:** Example configurations (no secrets).
- **Accepted dependencies:** inherits `config/` rules (see parent README).
