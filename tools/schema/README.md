# tools/schema/

> **Submodule owner stub (M0.1).** Scaffold only — no logic yet.

- **Parent:** `tools/`
- **Purpose:** Schema/code generation and drift detection.
- **Accepted dependencies:** inherits `tools/` rules (see parent README).
